%---------------------------------------------------------------
% Main thesis text
%---------------------------------------------------------------

%---------------------------------------------------------------
\chapter{Introduction}
%---------------------------------------------------------------
\setcounter{page}{1}

\begin{chapterabstract}
This thesis addresses problem of deploying and evaluating
modern neural networks designed for detecting facial landmarks directly on mobile devices. 
The work focuses on the theoretical foundations and experimental methodology required
for benchmarking of face detection and facial landmark estimation
on Android and iOS using ExecuTorch and backend delegates.
\end{chapterabstract}

\section{Motivation}

On-device inference has become a key requirement for many applications, because 
it improves privacy, reduces round-trip latency, and
allows offloading compute resource to edge devices. At the same time, mobile devices are constrained by
strict limits on compute throughput, memory bandwidth, power, and thermal
budget \cite{wang2020mobilesoc,li2024mobilebenchmark}. In practice, this
means that achieving good model accuracy is not sufficient; the full
end-to-end system must also be efficient.

Recent community benchmarks confirm that measured latency strongly depends on
hardware configuration, runtime backend, numerical precision. \cite{reddi2022mlperfmobile,li2024mobilebenchmark}.

\section{Problem statement}

The core of this thesis is to evaluate neural networks for detecting facial landmarks on mobile devices, with a focus on configurations 
implemented with ExecuTorch. The benchmark must support equivalent iOS and 
Android applications, consistent measurement conditions, and validation of quality metrics.

The benchmark is a two-stage pipeline:
\begin{enumerate}
    \item face detection in an input image,
    \item facial landmark estimation on detected face crops.
\end{enumerate}

\section{Research objectives}

Objectives of the thesis are:
\begin{itemize}
    \item summarize practical options for running neural networks on Android and
          iOS;
    \item select suitable face-processing models (detector + landmark regressor)
          for mobile real-time use;
    \item implement equivalent benchmark scenarios on both platforms;
    \item evaluate latency (ms/frame), throughput (FPS), and landmark accuracy;
    \item analyze INT8 PTQ optimization impact on speed and quality.
\end{itemize}

Citations and bibliography entries in this thesis are formatted according to
ČSN ISO 690 (numeric method) using the \texttt{biblatex-iso690} style.

\section{Alignment with thesis assignment tasks}

The assignment tasks are covered as follows:
\begin{enumerate}
    \item \textbf{Mobile NN deployment options:} Chapter~2 and Chapter~3 review
          mobile hardware/runtime constraints and ExecuTorch backend options.
    \item \textbf{Model selection + test inputs:} Chapter~2 (model section)
          explains selected detector/landmark architectures; Chapter~4 defines
          the reproducible AFW-based input protocol used for quantitative
          comparison.
    \item \textbf{Android/iOS testing scenario:} Chapter~4 documents feature
          parity and unified measurement schema for both mobile apps.
    \item \textbf{Latency/FPS (and optional energy):} Chapter~4 defines metrics
          and logging; Chapter~5 reports results measured on various devices.
    \item \textbf{Optimization impact (quantization):} Chapter~3 explains PTQ
          export workflow and Chapter~5 compares FP32 vs INT8 behavior.
\end{enumerate}

%---------------------------------------------------------------
\chapter{Theoretical background}
%---------------------------------------------------------------

\section{On-device inference in mobile environments}

Mobile AI systems are typically deployed on heterogeneous Systems-on-Chip
(SoCs), where CPU, GPU, and dedicated NPUs coexist
\cite{lee2019mobilegpu,wang2020mobilesoc}. Compared to server inference,
mobile workloads operate under tighter thermal and power limits and often share
resources with other tasks.

Latency depends on several things:
\begin{itemize}
    \item model architecture (operator types, tensor shapes, parameter count),
    \item runtime backend (kernel library, operator fusion, threading policy),
    \item hardware mapping (CPU/GPU/NPU dispatch),
\end{itemize}

That means the same neural network can show very different
performance across devices and software stacks
\cite{li2024mobilebenchmark,reddi2022mlperfmobile}.

\section{Hardware and system constraints}

\subsection{CPU, GPU, and NPU roles}

CPUs provide broad operator coverage and predictable behavior, but may not offer
the highest throughput for dense tensor computations. Mobile GPUs can improve
throughput for selected workloads, while NPUs may deliver superior
energy-efficiency when supported operators map well to accelerator kernels
\cite{lee2019mobilegpu,wang2020mobilesoc}. In real applications, unsupported
subgraphs frequently fall back to CPU, and thus synchronization and memory
transfer overhead become issues.

\subsection{Power and thermal behavior}

Peak performance is often not sustainable in mobile devices. Very long
inference can trigger thermal throttling, reducing frequency and increasing
latency \cite{li2024mobilebenchmark}.

\section{Efficient neural networks for mobile vision}

The shift from CNNs deployed on servers to mobile-friendly architectures was enabled
by operator-level efficiency ideas such as depthwise separable convolution,
inverted residual blocks, and channel shuffle
\cite{howard2017mobilenets,sandler2018mobilenetv2,zhang2018shufflenet}.
SqueezeNet demonstrated that competitive accuracy can be achieved with
significantly fewer parameters \cite{iandola2016squeezenet}.

For processing faces on mobile devices, both detector and landmark models must be efficient.

\section{Model compression and quantization}

Compression and quantization reduce memory footprint and often improve runtime
performance. The most common compression techniques include pruning and weight sharing
\cite{han2016deepcompression}. In practical mobile deployment, integer
quantization is especially important because INT8 kernels can be faster and more
energy-efficient than FP32 on supported hardware \cite{jacob2018quantization}.

\subsection{What PTQ does}

Post-training quantization (PTQ) converts an already trained FP32 model to a
lower-precision representation, usually without retraining. In this thesis, the
main target is INT8 execution. A real value $x$ is represented by an integer
$q$ using scale $s$ and zero-point $z$:
\begin{equation}
q = \mathrm{clip}\!\left(\mathrm{round}\!\left(\frac{x}{s}\right) + z,\; q_{\min}, q_{\max}\right),
\qquad
\hat{x} = s\,(q-z).
\end{equation}

For signed INT8, $q$ is typically in $[-128,127]$ (or $[-127,127]$ in symmetric
weight quantization). The reconstructed value $\hat{x}$ approximates $x$, so a
small quantization error is introduced.

PTQ typically has three steps:
\begin{enumerate}
    \item prepare the graph for quantization,
    \item run calibration data through the model to estimate activation ranges,
    \item convert operators to quantized kernels.
\end{enumerate}

Compared to quantization-aware training (QAT), PTQ is much simpler to apply,
but it is more sensitive to calibration quality and to model-specific outliers.

\subsection{INT8 design choices that affect quality}

Two design choices are central for this work:
\begin{itemize}
    \item \textbf{Per-tensor vs per-channel weights:} per-channel assigns
          separate scales per output channel and usually preserves accuracy
          better, especially in convolution-heavy models.
    \item \textbf{Static vs dynamic activations:} static PTQ calibrates
          activation ranges offline; dynamic approaches estimate part of the
          range online. Static PTQ is often faster when backend support is
          good.
\end{itemize}

INT8 often improves speed for two reasons: lower memory traffic (INT8 tensors
are 4$\times$ smaller than FP32) and faster vectorized kernels. However, real
inference latency reduction depends on operator coverage. If part of the graph falls back
to non-quantized execution, conversion overhead can reduce the expected speedup
\cite{krishnamoorthi2018quantizing}.

\section{Face detection and landmark estimation}

\subsection{Task definitions}

Face detection finds faces with bounding boxes. Landmark estimation predicts
keypoint coordinates (e.g., eye corners, nose tip, mouth corners), usually on a
cropped face image. These tasks are strongly coupled:
detection quality and crop geometry directly influence landmark quality.

\subsection{Representative models}

RetinaFace is a strong one-stage detector with high robustness under challenging
conditions \cite{deng2020retinaface}. FaceBoxes is a lightweight detector
explicitly designed for CPU real-time operation \cite{zhang2017faceboxes}.

For landmarks, PFLD emphasizes compactness and speed \cite{guo2019pfld}.
MobileFaceNet provides a lightweight backbone originally developed for mobile
face recognition and often reused for landmark regression
\cite{chen2018mobilefacenets}. PIPNet combines heatmap-style reasoning with
low-resolution efficient heads for practical landmark localization
\cite{jin2021pipnet}.

In the benchmark matrix used in this thesis, the main detector+landmark
combinations are based on RetinaFace-MNet0.25 or FaceBoxesV2 with
MobileFaceNet, PFLD-MNv2-0.25, PFLD-MNv2-1.0, and PipNet-ResNet18.

\begin{table}[htbp]
\centering
\small
\begin{tabular}{p{2.8cm}p{1.5cm}p{1.7cm}p{6.2cm}}
\hline
Model & Stage & Relative cost & Main properties \\
\hline
RetinaFace-MNet0.25 & Detector & Medium & Robust detection quality; higher detector cost than very light alternatives. \\
FaceBoxesV2 & Detector & Low & Very fast detector; useful when throughput is prioritized. \\
MobileFaceNet & Landmark & Low & Lightweight baseline; stable and efficient for mobile CPUs. \\
PFLD-MNv2-0.25 & Landmark & Low & Compact model with good speed and moderate landmark precision. \\
PFLD-MNv2-1.0 & Landmark & Medium & Higher capacity than 0.25 variant; better potential quality at higher compute cost. \\
PipNet-ResNet18 & Landmark & High & Stronger landmark localization capacity, but substantially heavier compute load. \\
\hline
\end{tabular}
\caption{Qualitative properties of models used in the benchmark matrix.}
\label{tab:model-properties-qual}
\end{table}

\subsection{Datasets and annotation uncertainty}

In this thesis, AFW annotations are used for wild-condition landmark evaluation,
originating from the face detection and pose estimation work of Zhu and
Ramanan \cite{zhu2012facedetectionwild}.

%---------------------------------------------------------------
\chapter{ExecuTorch for mobile deployment}
%---------------------------------------------------------------

\section{Why ExecuTorch}

ExecuTorch is a PyTorch edge deployment stack designed for efficient on-device
inference across mobile and embedded devices
\cite{executorch_how,executorch_arch}. Its central design principle is
\emph{ahead-of-time} (AOT) preparation: expensive transformation and planning
steps are performed before deployment, so runtime execution remains lightweight.

\section{Conceptual architecture}

ExecuTorch organizes model deployment into three phases
\cite{executorch_how,executorch_arch}:
\begin{enumerate}
    \item export a PyTorch model to graph form,
    \item lower and optimize the graph for edge execution,
    \item execute the serialized program with a small C++ runtime.
\end{enumerate}

\section{Export and lowering workflow}

The practical workflow consists of:
\begin{enumerate}
    \item model export using \texttt{torch.export},
    \item backend specific lowering via
          \texttt{to\_edge\_transform\_and\_lower},
    \item creation of a serialized \texttt{.pte} artifact.
\end{enumerate}

This process supports dynamic shape constraints and optional separation of
program and tensor data into \texttt{.pte} and \texttt{.ptd} files
\cite{executorch_export,executorch_pte,executorch_ptd,pytorch_export}.

\section{Delegation and partitioning}

ExecuTorch uses delegates to offload supported subgraphs to backend specific
implementations \cite{executorch_delegate}. A partitioner tags lowerable graph
regions; unsupported regions remain in portable execution. This mixed execution
model improves portability but can create overhead.

For this thesis, two backends are central:
\begin{itemize}
    \item \textbf{XNNPACK}: optimized CPU backend with broad mobile support,
          including quantized paths \cite{executorch_xnnpack,xnnpack_repo};
    \item \textbf{Core ML delegate}: Apple backend path that can dispatch to CPU,
          GPU, or ANE depending on model and platform capabilities
          \cite{executorch_coreml,apple_coreml}.
\end{itemize}

\section{Quantization in ExecuTorch}

ExecuTorch implements quantization through backend specific
configurations, commonly using PT2E/torchao-based preparation and conversion
\cite{executorch_quantization,torchao_pt2e}. In this project, INT8 exports are
produced for XNNPACK by
\texttt{models/export\_landmark\_models\_xnnpack\_quantized.py}, which does
symmetric PTQ.

\subsection{What INT8 PTQ means in this deployment pipeline}

Simply put, the exported graph replaces supported FP32 operators with
INT8 kernels (mainly convolution and linear-family operations in XNNPACK). The
internal arithmetic is typically INT8$\times$INT8 with INT32 accumulation,
followed by requantization to the next expected tensor format.

This usually helps mobile inference because:
\begin{itemize}
    \item weights and (for static paths) activations use less memory,
    \item cache locality improves,
    \item backend kernels can use optimized vector instructions.
\end{itemize}

However, measured gains depend on full-graph compatibility. If unsupported
operators force fallback segments, extra quantize/dequantize boundaries appear
and reduce net speed benefit. This explains why INT8 speedups in the
results chapter are not uniform across all models.

For landmark regression, INT8 error sensitivity is depends on the model: compact or
outlier-sensitive layers can produce NME degradation if calibration is not
representative.

\subsection{Implemented PTQ workflow in the export scripts}

For each model and selected quantization method, the script runs:
\begin{enumerate}
    \item build XNNPACK quantizer configuration,
    \item prepare quantized graph with \texttt{prepare\_pt2e},
    \item run calibration forward passes,
    \item convert to quantized graph with \texttt{convert\_pt2e},
    \item lower to ExecuTorch with XNNPACK partitioner and serialize \texttt{.pte}.
\end{enumerate}

The INT8 methods implemented:
\begin{itemize}
    \item \texttt{pc}: per-channel weight quantization
          (file suffix \texttt{\_xnnpack\_int8}),
\end{itemize}

The quantizer setup is created by
\texttt{get\_symmetric\_quantization\_config(is\_per\_channel, is\_dynamic)}.
And the key internal PTQ parameters are:
\begin{itemize}
    \item \texttt{is\_per\_channel} $\in \{\texttt{True},\texttt{False}\}$,
    \item \texttt{is\_dynamic} $\in \{\texttt{True},\texttt{False}\}$.
\end{itemize}

Static INT8 export uses a fixed sample input tensor and calibrates for
$S$ iterations, where
\begin{equation}
S = \texttt{XNNPACK\_INT8\_CALIBRATION\_SAMPLES}
\end{equation}
(default $S=32$).

Dynamic-batch INT8 export constrains only the batch dimension using
\texttt{torch.export.Dim("batch", min, max)} and calibrates representative
batch sizes from $\{b_{\min}, b_{\text{mid}}, b_{\max}\}$. In the script,
\begin{equation}
b_{\text{mid}} =
\begin{cases}
 b_{\min}, & b_{\min}>1,\\
 \min(2,b_{\max}), & b_{\min}=1.
\end{cases}
\end{equation}

\subsection{Model-specific safety workarounds used during quantized export}

The implemented export pipeline contains explicit safeguards discovered during
the export process to improve stability and quality of the generated INT8 artifacts. These include:
\begin{itemize}
    \item \textbf{MobileFaceNet output stabilization:} fusion of
          \texttt{Linear+BatchNorm1d} in the output head and export as a
          single landmark tensor output;
    \item \textbf{MobileFaceNet XNNPACK stability:} exclusion of
          \texttt{PreluConfig} from XNNPACK delegation to avoid unstable runtime
          behavior;
\end{itemize}

\subsection{Export-script parameters relevant to quantization}

The quantized export process is controlled through environment parameters:
\begin{itemize}
    \item \texttt{EXPORT\_BATCH\_SIZES}: static batch sizes to export
          (default in wrapper script: \texttt{1});
    \item \texttt{EXPORT\_DYNAMIC\_BATCH}: enable dynamic-batch exports
          (default: \texttt{1});
    \item \texttt{EXPORT\_DYNAMIC\_MIN\_BATCH}: lower dynamic batch bound
          (default: \texttt{1});
    \item \texttt{EXPORT\_DYNAMIC\_MAX\_BATCH}: upper dynamic batch bound
          (default: \texttt{128});
    \item \texttt{XNNPACK\_INT8\_CALIBRATION\_SAMPLES}: number of static
          calibration iterations (default: \texttt{32});
    \item \texttt{XNNPACK\_INT8\_METHODS}: selected INT8 methods
          (default: \texttt{pc,pt});
    \item \texttt{MOBILEFACENET\_STATIC\_INT8\_DYNAMIC\_ACT\_WORKAROUND}:
          enable MobileFaceNet static INT8 workaround (default: enabled);
    \item \texttt{PYTHON\_BIN}: interpreter used by wrapper shell script.
\end{itemize}

For completeness, the same export ecosystem also accepts backend-target
parameters used by non-quantized scripts
(\texttt{QNN\_SOC\_MODEL}, \texttt{MTK\_PLATFORM\_CONFIG},
\texttt{SAMSUNG\_CHIPSET}, \texttt{EXPORT\_APPLE\_BACKENDS}), which have an effect on
which delegate variants are generated but do not change INT8 calibration rules.

%---------------------------------------------------------------
\chapter{Benchmark methodology}
%---------------------------------------------------------------

\section{Methodological goals}

The benchmark design in this thesis follows two goals:
\begin{enumerate}
    \item produce comparable latency/throughput/accuracy measurements across
          iOS and Android,
    \item preserve raw artifacts needed for analysis of the results.
\end{enumerate}

\section{Evaluated pipeline}

Each benchmark run executes an end-to-end pipeline:
\begin{enumerate}
    \item detector preprocessing,
    \item detector inference,
    \item detector postprocessing (box decoding and filtering),
    \item face crop generation,
    \item landmark model preprocessing,
    \item landmark inference,
    \item landmark postprocessing.
\end{enumerate}

\section{Model families and configuration space}

The methodology supports multiple detector/landmark pairs and backend
configurations. Typical factors include:
\begin{itemize}
    \item detector choice (e.g., RetinaFace vs FaceBoxes),
    \item landmark architecture (e.g., PFLD, MobileFaceNet, PIPNet),
    \item precision variant (FP32 and INT8),
    \item backend delegate (XNNPACK, Core ML and portable),
    \item resolved batch size.
\end{itemize}

A complete run record stores these factors explicitly, so later analysis does
not depend on file naming conventions alone.

\section{iOS and Android implementation parity}

Both iOS and Android benchmark applications are designed 
to be feature-equivalent at the pipeline level.
Equivalent behavior includes:
\begin{itemize}
    \item identical preprocessing parameters,
    \item identical postprocessing logic and thresholds,
    \item consistent metric collection and export schema,
    \item consistent warm-up and measured iterations.
\end{itemize}

\section{Dataset and inputs}

AFW-based landmark annotations are used for evaluation of the accuracy \cite{zhu2012facedetectionwild}.

\section{Metrics}

\subsection{Landmark inference latency}

Although each run executes the full detector$+$landmark pipeline, the thesis
comparison charts are organized by the \emph{landmark} configuration
(model, backend, quantization, and resolved batch size). The primary timing
metric is therefore landmark \emph{inference} latency only:
\begin{equation}
\ell^{\text{lm-inf}}_i = t^{\text{end}}_{\text{lm-inf},i} - t^{\text{start}}_{\text{lm-inf},i}.
\end{equation}

For each run, this metric is extracted from per-iteration
\texttt{landmarkInferenceMs} samples and summarized in milliseconds by average,
$p50$, and $p95$. For charting, values are aggregated per configuration group
using the median across repeated runs:
\begin{equation}
\tilde{m}_g = \mathrm{median}\{m_r \mid r \in g\}.
\end{equation}

\subsection{Throughput and batch scaling}

Run throughput follows the exported timing summary:
\begin{equation}
\mathrm{FPS} = \frac{N_{\text{images}}}{T_{\text{run}}}.
\end{equation}

BS=1 charts compare median FPS by landmark configuration. Batch-scaling charts
plot median FPS as a function of resolved landmark batch size
($b \in \{1,2,4,8\}$ in the curated thesis set).

\subsection{Landmark accuracy}

Accuracy is recomputed offline from exported per-image predicted landmarks and
AFW ground truth annotations \cite{zhu2012facedetectionwild}. For $N$ evaluated images and $K$ landmarks,
inter-ocular NME is
\begin{equation}
\mathrm{NME}_{\text{iod}} = \frac{1}{N}\sum_{n=1}^{N}\frac{1}{K}\sum_{k=1}^{K}
\frac{\lVert \hat{\mathbf{p}}_{n,k} - \mathbf{p}_{n,k} \rVert_2}{d^{\text{iod}}_n},
\end{equation}
where $d^{\text{iod}}_n$ is the inter-ocular distance for image $n$.

\subsection{Optional energy metrics}

If platform tooling allows reliable collection, energy-related signals can be
logged as supplementary indicators. In this thesis, landmark-inference
latency, throughput, and landmark accuracy remain primary. Energy metrics are very hard to obtain.
They are not included in the main results chapter, but they are collected when possible and stored in the raw artifacts for future analysis.
\section{Measurement protocol}

To reduce noise and measurement bias, each benchmark condition follows a fixed
protocol:
\begin{itemize}
    \item dedicated warm-up iterations before timed measurement,
    \item fixed number of measured iterations,
    \item stable device state (charging policy, background app control,
          thermal preconditioning),
    \item separate logging for initialization, inference, and optional
          postprocessing stages.
\end{itemize}

\section{Run artifacts and offline analysis}

Every run exports a machine-readable summary plus raw samples. Offline analysis
scripts recompute aggregate statistics and recompute accuracy
from image predictions instead of trusting prefilled summary fields. This allows for more flexible and transparent analysis, 
as well as the possibility to add new metrics without rerunning benchmarks.

\section{Threats to validity}

Main threats and mitigations include:
\begin{itemize}
    \item \textbf{Thermal drift}: controlled warm-up and repeated runs;
    \item \textbf{Backend asymmetry}: shared-configuration comparisons for
          cross-device claims;
    \item \textbf{Unsupported operators}: explicit reporting of delegate
          coverage and fallback paths;
\end{itemize}

%---------------------------------------------------------------
\chapter{Experimental results}
%---------------------------------------------------------------

\section{Result scope and source artifacts}

The main simplified dashboard is configured around one focus mobile device
(Xiaomi Redmi Note 8 Pro) with PC reference rows. In addition iPhone 14 results are included.

\section{BS=1 speed and latency overview (PC vs focus device)}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/simple_01_bs1_fps_pc_vs_focus_device.png}
    \caption{BS=1 median FPS: PC vs focus device (from combined charts).}
    \label{fig:simple-bs1-fps}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/simple_03_bs1_latency_pc_vs_focus_device.png}
    \caption{BS=1 median landmark-inference latency: PC vs focus device (from combined charts).}
    \label{fig:simple-bs1-latency}
\end{figure}

Figures~\ref{fig:simple-bs1-fps} and~\ref{fig:simple-bs1-latency} quantify the
gap between desktop and mobile execution at BS=1. The PC reaches roughly
16--25~FPS across all four landmark models, while the focus phone falls into a
much narrower 3--8~FPS band. The gap is not uniform: it is smallest for the
lightweight PFLD-MNv2-0.25 (PC/phone $\approx$~2.0$\times$ in FPS) and largest
for PipNet-R18 in FP32, where the PC is roughly an order of magnitude faster
($24.86$ vs $2.28$~FPS, i.e.\ $\sim$10.9$\times$).

\section{Focus-device backend and quantization at BS=1}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_01_bs1_fps_backend_quant.png}
    \caption{Focus device: BS=1 speed by backend and quantization.}
    \label{fig:focus-bs1-fps}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_02_bs1_accuracy_backend_quant.png}
    \caption{Focus device: BS=1 NME accuracy by backend and quantization (lower is better).}
    \label{fig:focus-bs1-nme}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Model & Quant & FPS & LM inf. [ms] & E2E [ms] & NME \\
\hline
MobileFaceNet      & FP32 & 3.39 & 184.33 & 277.18 & 0.0428 \\
MobileFaceNet      & INT8 & 3.46 & 178.39 & 272.20 & 0.0428 \\
PFLD-MNv2-0.25     & FP32 & 8.04 & 9.99   & 107.34 & 0.0978 \\
PFLD-MNv2-0.25     & INT8 & 8.24 & 7.10   & 104.81 & 0.1039 \\
PFLD-MNv2-1.0      & FP32 & 5.82 & 61.54  & 155.65 & 0.0798 \\
PFLD-MNv2-1.0      & INT8 & 7.22 & 23.78  & 121.56 & 0.1014 \\
PipNet-R18         & FP32 & 2.28 & 351.97 & 421.52 & 0.0446 \\
PipNet-R18         & INT8 & 5.53 & 97.46  & 164.59 & 0.0463 \\
\hline
\end{tabular}
\caption{Focus-device BS=1 XNNPACK metrics (raw values).}
\label{tab:focus-bs1-raw}
\end{table}

PipNet-R18 gains 2.42$\times$ in FPS ($2.28 \rightarrow 5.53$) and
its landmark inference drops from $352$~ms to $97$~ms (a 3.6$\times$
reduction); PFLD-MNv2-1.0 gains 1.24$\times$ in FPS but 2.6$\times$ in
landmark latency ($61.5 \rightarrow 23.8$~ms).

The accuracy picture is correspondingly model-dependent. NME for MobileFaceNet
is essentially unchanged ($0.0428 \rightarrow 0.0428$, $<0.1\%$ relative) and
PipNet-R18 only degrades by $\sim$3.7\% relative ($0.0446 \rightarrow 0.0463$).
The PFLD family is more sensitive: PFLD-MNv2-0.25 loses $\sim$6\% relative NME
($0.0978 \rightarrow 0.1039$) and the heavier PFLD-MNv2-1.0 loses $\sim$27\%
relative ($0.0798 \rightarrow 0.1014$). The PFLD-MNv2-1.0 case is the
canonical speed/accuracy trade-off in this benchmark: a 24\% FPS gain costs a
27\% NME increase, which may or may not be acceptable depending on the
actual use case. PipNet-R18 INT8, by contrast, is a near-Pareto win---the
largest speed gain in the matrix accompanied by a barely measurable accuracy
loss---making it the strongest INT8 candidate among the tested models on this
device.

\section{Batch scaling on focus device (BS=1,2,4,8)}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_03_batch_scaling_fps__pfld_mobilenetv2_10.png}
    \caption{Focus device batch scaling for PFLD-MNv2-1.0 (XNNPACK variants).}
    \label{fig:focus-batch-pfld10}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_03_batch_scaling_fps__pipnet_resnet18.png}
    \caption{Focus device batch scaling for PipNet ResNet18 (XNNPACK variants).}
    \label{fig:focus-batch-pipnet}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Model & Quant & FPS@1 & FPS@2 & FPS@4 & FPS@8 \\
\hline
PFLD-MNv2-1.0 & FP32 & 5.82 & 5.53 & 5.44 & 5.97 \\
PFLD-MNv2-1.0 & INT8 & 7.22 & 7.50 & 7.49 & 7.66 \\
PipNet-R18    & FP32 & 2.28 & 2.22 & 2.31 & 2.27 \\
PipNet-R18    & INT8 & 5.53 & 5.34 & 5.52 & 5.66 \\
\hline
\end{tabular}
\caption{Focus-device batch scaling in FPS (raw values).}
\label{tab:focus-batch-fps}
\end{table}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Model & Quant & LM ms@1 & LM ms@2 & LM ms@4 & LM ms@8 \\
\hline
PFLD-MNv2-1.0 & FP32 & 61.54 & 37.19 & 24.78 & 16.03 \\
PFLD-MNv2-1.0 & INT8 & 23.78 & 11.81 & 7.37  & 5.29 \\
PipNet-R18    & FP32 & 351.97 & 198.58 & 132.34 & 103.89 \\
PipNet-R18    & INT8 & 97.46  & 55.81  & 37.10  & 27.26 \\
\hline
\end{tabular}
\caption{Focus-device batch scaling in landmark-inference latency (raw values).}
\label{tab:focus-batch-lm-ms}
\end{table}

FPS for PFLD-MNv2-1.0 stays in a tight $5.4$--$6.0$
range (FP32) and $7.2$--$7.7$ range (INT8) from BS=1 to BS=8, while PipNet-R18
hovers at $\sim$2.3~FPS (FP32) and $\sim$5.4--5.7~FPS (INT8). In contrast,
per-image landmark inference latency falls almost linearly with batch size:
PFLD-MNv2-1.0 INT8 drops from $23.78$~ms to $5.29$~ms ($4.5\times$ across
$8\times$ batch), and PipNet-R18 INT8 from $97.46$~ms to $27.26$~ms
($3.6\times$).

\section{Quantization deltas and cumulative error behavior}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_04_quant_vs_unquant_speedup_bs1.png}
    \caption{Focus device: INT8 vs FP32 speed comparison at BS=1.}
    \label{fig:focus-int8-speed}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_05_quant_vs_unquant_accuracy_delta_bs1.png}
    \caption{Focus device: INT8 vs FP32 NME difference at BS=1.}
    \label{fig:focus-int8-accuracy-delta}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/focus_07_cumulative_hist_nme_bs1.png}
    \caption{Focus device cumulative NME curves for BS=1 XNNPACK FP32/INT8 runs.}
    \label{fig:focus-cdf-nme}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Model & PC INT8 FPS & Focus INT8 FPS & PC E2E [ms] & Focus E2E [ms] \\
\hline
MobileFaceNet      & 16.75 & 3.46 & 59.64 & 272.20 \\
PFLD-MNv2-0.25     & 16.63 & 8.24 & 59.92 & 104.81 \\
PFLD-MNv2-1.0      & 16.35 & 7.22 & 61.01 & 121.56 \\
PipNet-R18         & 24.86 & 5.53 & 40.19 & 164.59 \\
\hline
\end{tabular}
\caption{BS=1 PC INT8 and focus-device INT8 raw metrics.}
\label{tab:pc-vs-focus-raw}
\end{table}

Figure~\ref{fig:focus-int8-speed} summarises the BS=1 INT8/FP32 FPS ratio per
model and makes the two-regime story visually explicit: MobileFaceNet and
PFLD-MNv2-0.25 sit on the unity line ($1.02\times$ and $1.02\times$),
PFLD-MNv2-1.0 reaches $1.24\times$, and PipNet-R18 is the clear outlier at
$2.42\times$. Combined with the cumulative NME curves
(Figure~\ref{fig:focus-cdf-nme}), where the FP32 and INT8 distributions
overlap closely for MobileFaceNet and PipNet-R18 but separate visibly for
PFLD-MNv2-1.0, the data supports a simple deployment heuristic: prefer INT8
unconditionally for PipNet-R18, for
MobileFaceNet we can consider INT8 quantization as a free but small win , 
and for the PFLD family models we have to consider if the accuracy/throughput tradeoff is worth it.

Table~\ref{tab:pc-vs-focus-raw} shows the absolute
deployment gap. After INT8 quantization on both sides, PC throughput is still
$2.0\times$--$4.8\times$ higher than the phone, and the gap is largest for
the smallest models (MobileFaceNet at $4.85\times$).

\section{iPhone 14 charts, tables, and interpretation}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/simple_01_bs1_fps_focus_device.png}
    \caption{iPhone 14: BS=1 median FPS (device-focused chart pack).}
    \label{fig:iphone-simple-fps}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/simple_03_bs1_latency_focus_device.png}
    \caption{iPhone 14: BS=1 median landmark-inference latency.}
    \label{fig:iphone-simple-latency}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/focus_01_bs1_fps_backend_quant.png}
    \caption{iPhone 14: BS=1 speed by backend and quantization.}
    \label{fig:iphone-focus-backend}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/focus_04_quant_vs_unquant_speedup_bs1.png}
    \caption{iPhone 14: INT8 and FP32 speed comparison at BS=1.}
    \label{fig:iphone-int8-speed}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Model & Core ML FPS & XNN FP32 FPS & XNN INT8 FPS & Portable FPS \\
\hline
MobileFaceNet      & 8.45 & 5.14 & 5.38 & 0.30 \\
PFLD-MNv2-0.25     & 8.63 & 6.59 & 6.76 & 0.50 \\
PFLD-MNv2-1.0      & 8.51 & 5.78 & 6.34 & 0.25 \\
PipNet-R18         & 7.08 & 2.81 & 4.94 & 0.07 \\
\hline
\end{tabular}
\caption{iPhone 14 BS=1 throughput by backend (raw values).}
\label{tab:iphone-bs1-fps}
\end{table}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Model & Core ML E2E [ms] & XNN FP32 E2E [ms] & XNN INT8 E2E [ms] & Portable E2E [ms] \\
\hline
MobileFaceNet      & 107.35 & 177.74 & 169.33 & 3374.61 \\
PFLD-MNv2-0.25     & 107.89 & 135.38 & 135.90 & 1999.97 \\
PFLD-MNv2-1.0      & 109.89 & 160.36 & 141.19 & 3962.29 \\
PipNet-R18         & 133.35 & 338.35 & 186.01 & 13698.42 \\
\hline
\end{tabular}
\caption{iPhone 14 BS=1 end-to-end latency by backend (raw values).}
\label{tab:iphone-bs1-e2e}
\end{table}

Tables~\ref{tab:iphone-bs1-fps} and~\ref{tab:iphone-bs1-e2e} the difference between 
three different backend delegates on iPhone 14. The most striking pattern is that Core
ML delivers a remarkably uniform $7.1$--$8.6$~FPS across all four landmark
models---from the smallest PFLD-MNv2-0.25 to the heaviest PipNet-R18---which
is only possible because Apple's accelerated kernels reduce the landmark
forward pass to essentially zero (median landmark inference of $0.34$--$1.43$~ms
for Core ML in the underlying CSV). At that point, the bottleneck moves
entirely to the detector (RetinaFace / FaceBoxesV2) and the surrounding
pre/post-processing. This is a different operating point from XNNPACK,
where landmark inference is still measurable (4--44~ms in FP32) and therefore
still tunable through quantization.

The XNNPACK-vs-Portable comparison shows the cost of giving up
backend-specific acceleration: Portable is $17$--$97\times$ slower than Core
ML on the same model ranging from $17.3\times$ for PFLD-MNv2-0.25 to $97.1\times$ for PipNet-R18.
Portable is therefore unsuitable for any deployed configuration; it is only
useful as a correctness baseline.

The INT8/FP32 split on XNNPACK follows the same model-dependent pattern as on
the Xiaomi: PipNet-R18 is again the largest winner (FPS $2.81 \rightarrow
4.94$, $1.76\times$; landmark inference $44.5 \rightarrow 9.7$~ms,
$4.6\times$), PFLD-MNv2-1.0 moves modestly ($1.10\times$ FPS, $3.4\times$
landmark inference), and the smaller models barely move ($\leq 1.05\times$
FPS).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/focus_03_batch_scaling_fps__pfld_mobilenetv2_10.png}
    \caption{iPhone 14 batch scaling for PFLD-MNv2-1.0.}
    \label{fig:iphone-batch-pfld10}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{../benchmarks/results/combined/charts/device_iphone_14/focus_03_batch_scaling_fps__pipnet_resnet18.png}
    \caption{iPhone 14 batch scaling for PipNet ResNet18.}
    \label{fig:iphone-batch-pipnet}
\end{figure}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Model & Quant & FPS@1 & FPS@2 & FPS@4 & FPS@8 \\
\hline
PFLD-MNv2-1.0 & FP32 & 5.78 & 5.83 & 5.88 & 5.91 \\
PFLD-MNv2-1.0 & INT8 & 6.34 & 6.24 & 6.37 & 6.51 \\
PipNet-R18    & FP32 & 2.81 & 2.85 & 2.85 & 2.93 \\
PipNet-R18    & INT8 & 4.94 & 5.02 & 4.96 & 5.00 \\
\hline
\end{tabular}
\caption{iPhone 14 batch scaling in FPS (raw values).}
\label{tab:iphone-batch-fps}
\end{table}

\begin{table}[htbp]
\centering
\small
\begin{tabular}{llrrrr}
\hline
Model & Quant & LM ms@1 & LM ms@2 & LM ms@4 & LM ms@8 \\
\hline
PFLD-MNv2-1.0 & FP32 & 4.98 & 2.40 & 1.18 & 0.59 \\
PFLD-MNv2-1.0 & INT8 & 1.47 & 0.72 & 0.36 & 0.17 \\
PipNet-R18    & FP32 & 44.45 & 22.20 & 11.26 & 5.51 \\
PipNet-R18    & INT8 & 9.66 & 4.82 & 2.46 & 1.22 \\
\hline
\end{tabular}
\caption{iPhone 14 batch scaling in landmark-inference latency (raw values).}
\label{tab:iphone-batch-lm}
\end{table}

The iPhone 14 batch-scaling pattern is qualitatively the same as on the
Xiaomi.
PFLD-MNv2-1.0 INT8 lands at $0.17$~ms per image at BS=8, and PipNet-R18 INT8
at $1.22$~ms; in both cases the landmark forward has effectively been
amortised away. End-to-end FPS, however, only drifts from $5.78$ to $5.91$
for PFLD-MNv2-1.0 FP32 and from $2.81$ to $2.93$ for PipNet-R18 FP32.

\begin{table}[htbp]
\centering
\small
\begin{tabular}{lrrrr}
\hline
Model & PC INT8 FPS & iPhone INT8 FPS & PC E2E [ms] & iPhone E2E [ms] \\
\hline
MobileFaceNet      & 16.75 & 5.38 & 59.64 & 169.33 \\
PFLD-MNv2-0.25     & 16.63 & 6.76 & 59.92 & 135.90 \\
PFLD-MNv2-1.0      & 16.35 & 6.34 & 61.01 & 141.19 \\
PipNet-R18         & 24.86 & 4.94 & 40.19 & 186.01 \\
\hline
\end{tabular}
\caption{BS=1 PC INT8 and iPhone 14 INT8 raw metrics.}
\label{tab:pc-vs-iphone-raw}
\end{table}

Table~\ref{tab:pc-vs-iphone-raw} sets the deployment context. With the same
INT8 XNNPACK configuration on both sides, the PC is $2.46\times$ faster on
PFLD-MNv2-0.25, $2.58\times$ on PFLD-MNv2-1.0, $3.12\times$ on MobileFaceNet,
and $5.03\times$ on PipNet-R18.

%---------------------------------------------------------------
\chapter{Conclusion}
%---------------------------------------------------------------

This thesis presented a full benchmark for landmark detection models on mobile devices
with ExecuTorch, from theoretical background and export details to
cross platform runtime evaluation.

The thesis delivered:
\begin{itemize}
    \item implementation and comparison of mobile inference paths on both iOS
          and Android,
    \item evaluation of real-time-oriented metrics (ms/frame and FPS),
    \item multi-device comparison with reproducible logging and offline analysis,
    \item quantization study (INT8 PTQ) with speed/accuracy trade-off analysis,
    \item optional energy-related telemetry as auxiliary data where available.
\end{itemize}

The results show four main points:
\begin{itemize}
    \item backend and quantization selection strongly influence runtime;
    \item INT8 can provide major latency and FPS gains for heavier landmark
          models, but accuracy impact depends on the model;
    \item increasing batch size improves per-image landmark-inference latency
    \item PC reference runs are useful for capacity comparison
\end{itemize}

Overall, the implemented methodology and artifact pipeline provide a
reproducible base for future iterations, including model replacement, backend
retuning, and extension to more mobile devices.